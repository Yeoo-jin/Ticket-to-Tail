# Claude Code 프로젝트 지침

## 1. 작업 전 필수 확인

작업을 시작하기 전에 다음 파일을 먼저 읽는다.

- `README.md`
- `docs/idea.md`
- `docs/api-spec.md`
- `docs/feature-spec.md`

현재 프로젝트 구조와 기존 코드를 확인한 뒤 작업한다.

바로 코드를 수정하지 말고 먼저 다음 내용을 설명한다.

1. 작업 요청 이해
2. 수정할 파일
3. 새로 만들 파일
4. 기존 기능에 미치는 영향
5. 실행 및 확인 방법

사용자가 수정 계획을 확인한 뒤 코드를 변경한다.

---

## 2. 프로젝트 구조

```text
Ticket-to-Tale/
├─ frontend/
│  ├─ src/
│  │  ├─ components/
│  │  ├─ pages/
│  │  ├─ services/
│  │  ├─ data/
│  │  ├─ utils/
│  │  ├─ App.jsx
│  │  └─ main.jsx
│  ├─ public/
│  ├─ package.json
│  └─ .env.example
│
├─ backend/
│  ├─ app/
│  │  ├─ routes/
│  │  ├─ services/
│  │  ├─ prompts/
│  │  ├─ schemas/
│  │  ├─ data/
│  │  ├─ utils/
│  │  └─ main.py
│  ├─ requirements.txt
│  └─ .env.example
│
├─ docs/
├─ .gitignore
├─ CLAUDE.md
└─ README.md
```

기존 폴더 구조를 유지한다.

새 폴더 또는 새로운 구조가 필요하면 코드를 수정하기 전에 이유를 설명하고 사용자에게 확인받는다.

---

## 3. 기술 스택

### 프론트엔드

- React
- Vite
- Tailwind CSS
- JavaScript
- 모바일 웹 기반 반응형 UI

### 백엔드

- Python
- FastAPI
- REST API
- JSON 요청 및 응답
- 사진이 포함된 요청은 `multipart/form-data` 사용

사용자의 승인 없이 Next.js, Django, Flask 등 다른 프레임워크로 변경하지 않는다.

---

## 4. 프로젝트 주요 기능

1. 항공·철도 예매정보 텍스트 입력 및 구조화
2. 동행 조건 선택
3. 동행 조건 기반 관광지 후보 추천
4. 관광지 직접 선택 또는 AI 자동 선택
5. 다른 관광지 후보 다시 추천
6. 선택된 관광지 기반 맞춤 타임라인 생성
7. 사진·메모 기반 AI 여행 다이어리 생성

---

## 5. 공통 데이터 흐름

```text
예매정보 텍스트 입력
→ POST /api/bookings/parse
→ 동행 조건 선택
→ POST /api/places/recommend
→ 관광지 직접 선택 또는 AI 자동 선택
→ POST /api/timelines/generate
→ 사진·메모 입력
→ POST /api/diaries/generate
```

API 요청 및 응답 구조는 반드시 `docs/api-spec.md`를 기준으로 한다.

명세와 현재 코드가 다른 경우 임의로 판단하지 말고 사용자에게 먼저 알린다.

---

## 6. 필수 API

```text
POST /api/bookings/parse
POST /api/places/recommend
POST /api/timelines/generate
POST /api/diaries/generate
```

### 선택 구현 API

```text
POST /api/timelines/regenerate
POST /api/diaries/regenerate
```

API 경로, 요청 필드명 및 응답 필드명을 임의로 변경하지 않는다.

---

## 7. 파일 수정 기본 규칙

- 기존 파일을 임의로 삭제하지 않는다.
- 다른 담당자의 파일을 대규모로 수정하지 않는다.
- 한 번에 하나의 기능만 구현한다.
- 요청받은 범위 안에서만 수정한다.
- 기존 기능이 유지되도록 수정한다.
- API 필드명과 폴더명을 임의로 변경하지 않는다.
- 요청 범위 밖의 리팩터링은 진행하지 않는다.
- 새 라이브러리를 설치하기 전에 필요성과 영향을 설명한다.
- 이미 존재하는 컴포넌트, 함수 및 스타일을 우선 재사용한다.
- 비슷한 파일이나 기능을 중복으로 만들지 않는다.
- 수정 전 현재 브랜치와 변경사항을 확인한다.

---

## 8. 프론트엔드 규칙

- 화면 단위 파일은 `frontend/src/pages/`에 작성한다.
- 여러 화면에서 재사용하는 UI는 `frontend/src/components/`에 작성한다.
- API 호출 함수는 `frontend/src/services/`에 작성한다.
- 화면 파일마다 직접 `fetch` 또는 `axios` 코드를 반복하지 않는다.
- 임시 프론트 데이터는 `frontend/src/data/`에 작성한다.
- 공통 보조 함수는 `frontend/src/utils/`에 작성한다.
- 기존 공통 컴포넌트와 스타일을 우선 재사용한다.
- 모바일 화면을 우선으로 구현한다.
- 아이폰 Safari와 데스크톱 브라우저에서 사용할 수 있도록 구현한다.
- API 명세에 정의된 필드명을 그대로 사용한다.
- 백엔드 연결 전에는 API 응답과 동일한 구조의 목업 데이터를 사용한다.
- 목업 데이터와 실제 API 응답 구조가 다르지 않도록 한다.
- 화면 전체를 하나의 파일에 과도하게 작성하지 않는다.
- 반복되는 카드, 버튼 및 입력 요소는 컴포넌트로 분리한다.

---

## 9. 백엔드 규칙

- API 라우트는 `backend/app/routes/`에 작성한다.
- 핵심 처리 로직과 AI 호출은 `backend/app/services/`에 작성한다.
- AI 프롬프트는 `backend/app/prompts/`에 분리한다.
- 요청 및 응답 스키마는 `backend/app/schemas/`에 작성한다.
- 관광지 샘플 데이터는 `backend/app/data/places.json`에서 관리한다.
- 공통 보조 함수는 `backend/app/utils/`에 작성한다.
- 라우트 파일에 모든 처리 로직을 길게 작성하지 않는다.
- API 응답 구조는 `docs/api-spec.md`를 따른다.
- 예외 발생 시 공통 오류 응답 형식을 사용한다.
- 사용자 입력값을 검증한 뒤 AI 또는 서비스 로직에 전달한다.
- AI 응답을 그대로 반환하지 말고 명세에 맞는 구조로 정리한다.
- 실제 관광정보가 아닌 내용을 확정적인 사실처럼 생성하지 않는다.

---

## 10. 관광지 데이터 규칙

관광지 샘플 데이터는 다음 형식을 사용한다.

```json
{
  "placeId": "place-001",
  "name": "국립해양박물관",
  "description": "해양 문화를 체험할 수 있는 실내 관광지",
  "recommendationReason": "유아 동반 여행에 적합합니다.",
  "estimatedDurationMinutes": 90,
  "tags": [
    "실내",
    "유아 동반"
  ],
  "imageUrl": "/images/places/place-001.jpg"
}
```
### 관광지 필드 변경 금지

관광지 데이터의 다음 필드는 프로젝트의 공통 계약이므로 변경하지 않는다.

- `placeId`
- `name`
- `description`
- `recommendationReason`
- `estimatedDurationMinutes`
- `tags`
- `imageUrl`

다음과 같은 이름으로 임의 변경하지 않는다.

- `placeId` → `id`
- `recommendationReason` → `reason`
- `estimatedDurationMinutes` → `duration`
- `imageUrl` → `image`, `thumbnail`, `photoUrl`

새 필드가 필요하면 기존 필드를 삭제하거나 이름을 변경하지 않고 선택 필드로 추가한다.

필드 변경이 필요하다고 판단되면 코드를 수정하지 말고 다음 내용을 먼저 보고한다.

1. 변경이 필요한 이유
2. 영향을 받는 프론트엔드 파일
3. 영향을 받는 백엔드 파일
4. 영향을 받는 API 명세
5. 기존 데이터와의 호환 방법

사용자의 승인 없이 공통 필드명을 변경하지 않는다.

다른 장소 추천 기능에서는 사용자가 이미 선택한 관광지는 유지하고, 선택하지 않은 기존 후보만 새로운 후보로 교체한다.

---

## 11. 보안 규칙

- API 키를 코드에 직접 작성하지 않는다.
- `.env` 내용을 출력하거나 커밋하지 않는다.
- 개인정보를 코드 또는 데이터 파일에 저장하지 않는다.
- 실제 항공권·승차권 데이터를 저장소에 올리지 않는다.
- 비밀값은 환경변수로만 사용한다.
- 로그에 API 키를 출력하지 않는다.
- 사용자 입력 전체를 불필요하게 로그에 출력하지 않는다.
- 공개 저장소에 실제 계정 정보나 인증 토큰을 작성하지 않는다.
- 프론트엔드 코드에 AI API 키를 넣지 않는다.
- AI API 호출은 백엔드에서 처리한다.

---

## 12. Git 규칙

- `main` 브랜치에 직접 commit 또는 push하지 않는다.
- `git push --force`를 사용하지 않는다.
- 사용자의 요청 없이 commit, push, merge 또는 PR 생성을 하지 않는다.
- 작업 전에 `git status`를 확인한다.
- 작업 후 `git diff`를 확인한다.
- 변경한 파일 목록을 사용자에게 알려준다.
- 다른 팀원의 작업을 임의로 되돌리지 않는다.
- 충돌이 발생하면 강제로 해결하지 말고 사용자에게 알린다.
- 변경한 파일만 커밋 대상으로 제안한다.
- `.env`, `node_modules`, `venv` 등 제외 파일을 커밋하지 않는다.

---

## 13. 기능별 담당 범위

### 여진

- `backend/`
- `frontend/src/services/`
- AI 기능
- API 연결
- 공통 데이터 구조
- 프론트·백엔드 통합
- 배포

### 준영

- `backend/app/data/`
- `backend/app/prompts/`
- 관광지 추천 기준
- 동행 조건별 규칙
- 관광지 샘플 데이터
- 추천 결과 검수

### 서영

- `frontend/src/pages/`
- `frontend/src/components/`
- `frontend/src/assets/`
- 전체 프론트엔드
- UI/UX
- 모바일 반응형 화면

현재 작업자의 담당 범위를 벗어난 파일을 수정해야 하면 먼저 이유와 수정 범위를 설명하고 사용자에게 확인받는다.

---

## 14. Claude Code 작업 절차

작업 요청을 받으면 다음 순서를 따른다.

### 1단계: 문서 확인

다음 문서를 읽는다.

- `CLAUDE.md`
- `README.md`
- `docs/api-spec.md`
- `docs/idea.md`
- `docs/feature-spec.md`

### 2단계: 현재 상태 확인

다음을 확인한다.

- 현재 브랜치
- 기존 폴더 구조
- 관련 파일
- 현재 변경사항
- 이미 구현된 기능
- 재사용 가능한 코드

### 3단계: 수정 계획 보고

코드를 수정하기 전에 다음 내용을 사용자에게 설명한다.

1. 요청 이해
2. 수정할 파일
3. 새로 생성할 파일
4. 변경되는 데이터 흐름
5. 기존 기능에 미치는 영향
6. 테스트 방법

### 4단계: 사용자 확인 후 구현

사용자가 진행을 요청한 뒤 코드를 수정한다.

### 5단계: 실행 및 검증

프론트엔드 작업이면 빌드를 실행한다.

```bash
cd frontend
npm run build
```

백엔드 작업이면 서버 실행 또는 관련 테스트를 수행한다.

```bash
cd backend
uvicorn app.main:app --reload
```

### 6단계: 결과 보고

작업 완료 후 다음 내용을 보고한다.

1. 구현한 내용
2. 수정한 파일
3. 새로 생성한 파일
4. 실행한 명령어
5. 확인한 결과
6. 남아 있는 문제
7. 다음 연결 작업

---

## 15. Claude Code 사용 시 주의사항

- 한 번에 하나의 기능만 처리한다.
- “프로젝트 전체를 알아서 수정”하지 않는다.
- 관련 파일을 읽지 않고 새 코드를 만들지 않는다.
- 기존 구현이 있으면 최대한 재사용한다.
- 사용자의 승인 없이 파일을 삭제하지 않는다.
- 사용자의 승인 없이 폴더 구조를 변경하지 않는다.
- 사용자의 승인 없이 새로운 프레임워크를 도입하지 않는다.
- 새 패키지를 설치하기 전에 필요성을 설명한다.
- 공통 데이터 구조와 API 명세를 우선한다.
- 코드 작성 후 실제 실행 또는 빌드 결과를 확인한다.
- 실행하지 않은 코드를 완료되었다고 말하지 않는다.
- 오류가 발생하면 숨기지 말고 원인과 미해결 상태를 보고한다.
- 다른 팀원의 담당 파일을 수정해야 하면 먼저 확인받는다.
- `.env` 또는 비밀정보를 읽거나 출력하지 않는다.
- Git 명령으로 기존 작업을 삭제하거나 강제로 되돌리지 않는다.

---

## 16. 피해야 할 작업 요청 해석

다음과 같은 요청을 받더라도 프로젝트 전체를 임의로 수정하지 않는다.

```text
프로젝트 전체를 알아서 고쳐줘.
```

```text
다 지우고 새로 만들어줘.
```

```text
아무튼 에러 안 나게 해줘.
```

```text
알아서 구조 예쁘게 바꿔줘.
```

위와 같은 요청을 받으면 먼저 범위를 좁히고 수정 계획을 설명한다.

---

## 17. 작업 완료 조건

### 프론트엔드

다음 조건을 만족해야 한다.

- 화면이 정상적으로 렌더링된다.
- 모바일 크기에서 화면이 깨지지 않는다.
- 브라우저 콘솔에 치명적인 오류가 없다.
- API 또는 목업 데이터가 정상적으로 표시된다.
- 버튼과 화면 전환이 동작한다.
- 다음 명령이 성공한다.

```bash
cd frontend
npm run build
```

### 백엔드

다음 조건을 만족해야 한다.

- FastAPI 서버가 정상적으로 실행된다.
- `/docs`에서 API를 확인할 수 있다.
- 요청 스키마와 응답 구조가 명세와 일치한다.
- 입력값 오류가 적절하게 처리된다.
- API 키가 코드 또는 로그에 노출되지 않는다.
- 다음 명령으로 서버가 실행된다.

```bash
cd backend
uvicorn app.main:app --reload
```

---

## 18. 금지사항

- `main` 브랜치에 직접 push
- `git push --force` 사용
- 실제 API 키 커밋
- `.env` 파일 커밋
- 다른 담당자의 파일을 설명 없이 대규모 수정
- 폴더명과 API 필드명 임의 변경
- 기존 파일 무단 삭제
- 요청 범위 밖의 대규모 리팩터링
- 실행하지 않은 기능을 완료 처리
- 오류가 있는 상태로 완료 보고
- 공개 저장소에 개인정보 또는 실제 티켓 정보 업로드
- 팀원과 상의하지 않고 라이브러리 대량 설치
- 프론트엔드에 AI API 키 작성
- Claude Code가 수정한 내용을 확인하지 않고 커밋

---

## 19. Claude Code 첫 요청 예시

Claude Code를 프로젝트 루트에서 실행한 뒤 다음 내용을 입력한다.

```text
CLAUDE.md와 README.md, docs 폴더의 기능명세 및 API 명세를 먼저 읽어줘.

내 담당은 전체 프론트엔드야.

현재 프로젝트 구조와 이미 구현된 기능을 확인하고,
내 요청을 구현하려면 어떤 파일을 수정하거나 생성해야 하는지 먼저 알려줘.

기존 폴더 구조와 API 필드명은 변경하지 마.
다른 담당자의 파일은 수정하지 마.

아직 코드는 수정하지 마.
```

Claude Code가 수정 계획을 설명하면 내용을 확인한 뒤 다음과 같이 요청한다.

```text
방금 설명한 계획대로 구현해줘.

기존 기능을 유지하고 수정 범위를 최소화해줘.
API 명세에 정의된 필드명을 그대로 사용해줘.
내 담당 범위 밖의 파일은 수정하지 마.

작업 후 npm run build를 실행하고 다음 내용을 알려줘.

1. 구현한 내용
2. 수정한 파일
3. 새로 생성한 파일
4. 실행 결과
5. 남아 있는 문제
```

---

## 20. 작업 시작 전 체크리스트

```text
[ ] 현재 브랜치를 확인했다.
[ ] git status로 기존 변경사항을 확인했다.
[ ] CLAUDE.md와 README.md를 읽었다.
[ ] 기능명세와 API 명세를 확인했다.
[ ] 수정할 파일 범위를 정했다.
[ ] 다른 담당자의 파일이 포함되는지 확인했다.
[ ] API 키가 코드에 포함되지 않았는지 확인했다.
[ ] Claude Code가 제시한 수정 계획을 확인했다.
```

---

## 21. 작업 종료 전 체크리스트

```text
[ ] 기능을 직접 실행했다.
[ ] 브라우저 또는 API 문서에서 결과를 확인했다.
[ ] 오류 메시지가 없는지 확인했다.
[ ] git diff로 변경 내용을 확인했다.
[ ] 불필요한 파일이 수정되지 않았는지 확인했다.
[ ] API 명세와 필드명이 일치하는지 확인했다.
[ ] 실제 API 키가 포함되지 않았는지 확인했다.
[ ] 수정한 파일과 미완성 항목을 사용자에게 보고했다.
```
